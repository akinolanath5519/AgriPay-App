import "package:flutter/material.dart";


class BulkWeightScreen extends StatelessWidget {
  const BulkWeightScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Bulk Weight Page'),
    );
  }
}
