import "package:flutter/material.dart";

class CommoditiesScreen extends StatelessWidget {
  const CommoditiesScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const  Center(
      child: Text('Commodities Page'),
    );
  }
}
